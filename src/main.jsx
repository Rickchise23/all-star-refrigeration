import React from "react";
import ReactDOM from "react-dom/client";
import AllStarWebsite from "../allstar-website.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AllStarWebsite />
  </React.StrictMode>
);

